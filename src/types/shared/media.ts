/** Generic media / asset related shared types
 * This file intentionally holds enums & interfaces not tied to a specific game
 */

/**
 * Enum for image formats.
 */
export enum ImageFormat {
	WEBP = 'webp',
	PNG = 'png',
}

